!!! 5
mixin article(item)
  a( href="#{item.link}" )
    h1 #{item.title}
  h2.timestamp #{item.pretty_timestamp}
  p !{item.description}

html
  head
    title Simple News
    link(rel='stylesheet', href='/styles/style.css')
  body
    .wrapper
      for item in articles
        +article(item)
